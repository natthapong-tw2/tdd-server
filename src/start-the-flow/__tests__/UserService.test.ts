import { describe, it } from "vitest"

describe("UserService", () => {
  describe("createUser", () => {
    it("should return user id when create user success", () => {})
    it("should throw Technical Error when create user failed with database connection", () => {})
    it("should throw Business Error when create user failed with duplicated name", () => {})
  })
})
